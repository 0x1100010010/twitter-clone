<!DOCTYPE html>
<html>

<head>
  <title>TwitterClone</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <% if flash[:notice] %>
  <div class="alert-success">
    <p class="toast-body"><%= notice %></p>
  </div>
  <% else flash[:alert] %>
  <div class="alert-warning">
    <p class="toast-body"><%= alert %></p>
  </div>
  <% end %>

  <nav class="navbar sticky-top navbar-dark bg-dark navbar-expand-lg px-3 justify-content-between">
    <div><%= link_to 'Twitter Clone', root_path, class: 'navbar-brand' %></div>
    <div>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse navbar-end" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to 'Tweets', tweets_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'New_Tweet', new_tweet_path, class: 'nav-link' %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <%= yield %>
</body>

</html>